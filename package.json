{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.13",
    "tailwindcss": "^4.1.13"
  },
  "scripts": {
    "build": "npx tailwindcss -i ./assets/css/input.css -o ./assets/css/output.css --minify",
    "watch": "\"npx @tailwindcss/cli -i ./assets/css/style.css -o ./assets/css/output.css --watch\""
  },
  "_comments": {
    "build": "npm run build => Runs Tailwind once for production",
    "watch": "npm run watch => Runs Tailwind in watch mode for development"
  }
}